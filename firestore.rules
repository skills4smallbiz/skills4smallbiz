rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{path=*}/{userId} {
      allow read: if true;
      allow update: if request.auth.uid == userId;
    }
  }
}
